{% extends "__object.twig" %}

{% block objectContent %}
    <div class="image-content text-center">
        <img src="{{ image_url }}" alt="{{ title }}" style="max-width: 100%; max-height: 500px;">
    </div>
{% endblock %}